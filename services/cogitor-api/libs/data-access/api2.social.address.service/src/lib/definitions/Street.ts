import { HouseList } from './HouseList';

/**
 * Street
 * @targetNSAlias `s0`
 * @targetNamespace `https://www.portmonet.ru`
 */
export interface Street {
  /** HouseList */
  HouseList?: HouseList;
}
