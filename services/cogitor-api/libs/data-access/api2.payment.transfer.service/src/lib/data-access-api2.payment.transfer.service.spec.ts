import { dataAccessApi2PaymentTransferService } from './data-access-api2.payment.transfer.service';

describe('dataAccessApi2PaymentTransferService', () => {
  it('should work', () => {
    expect(dataAccessApi2PaymentTransferService()).toEqual(
      'data-access-api2.payment.transfer.service'
    );
  });
});
