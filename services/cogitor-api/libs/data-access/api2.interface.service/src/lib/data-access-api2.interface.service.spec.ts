import { dataAccessApi2InterfaceService } from './data-access-api2.interface.service';

describe('dataAccessApi2InterfaceService', () => {
  it('should work', () => {
    expect(dataAccessApi2InterfaceService()).toEqual(
      'data-access-api2.interface.service'
    );
  });
});
