import { dataAccessApi2Oauth2Service } from './data-access-api2.oauth2.service';

describe('dataAccessApi2Oauth2Service', () => {
  it('should work', () => {
    expect(dataAccessApi2Oauth2Service()).toEqual(
      'data-access-api2.oauth2.service'
    );
  });
});
