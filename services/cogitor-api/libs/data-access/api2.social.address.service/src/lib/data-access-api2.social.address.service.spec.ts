import { dataAccessApi2SocialAddressService } from './data-access-api2.social.address.service';

describe('dataAccessApi2SocialAddressService', () => {
  it('should work', () => {
    expect(dataAccessApi2SocialAddressService()).toEqual(
      'data-access-api2.social.address.service'
    );
  });
});
