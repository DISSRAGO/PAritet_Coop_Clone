import { Porch } from './Porch';

/**
 * ExtendedValues
 * @targetNSAlias `s0`
 * @targetNamespace `https://www.portmonet.ru`
 */
export interface ExtendedValues {
  /** ReferenceValue[] */
  ReferenceValue?: Array<Porch>;
}
