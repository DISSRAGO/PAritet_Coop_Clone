import { dataAccessApi2UserRegistrationService } from './data-access-api2.user.registration.service';

describe('dataAccessApi2UserRegistrationService', () => {
  it('should work', () => {
    expect(dataAccessApi2UserRegistrationService()).toEqual(
      'data-access-api2.user.registration.service'
    );
  });
});
