import { dataAccessApiExtParitetService } from './data-access-api.ext.paritet.service';

describe('dataAccessApiExtParitetService', () => {
  it('should work', () => {
    expect(dataAccessApiExtParitetService()).toEqual(
      'data-access-services.ext.paritet.service'
    );
  });
});
