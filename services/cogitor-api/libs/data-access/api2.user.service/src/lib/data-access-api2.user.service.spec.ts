import { dataAccessApi2UserService } from './data-access-api2.user.service';

describe('dataAccessApi2UserService', () => {
  it('should work', () => {
    expect(dataAccessApi2UserService()).toEqual(
      'data-access-api2.user.service'
    );
  });
});
